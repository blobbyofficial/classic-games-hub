{
  "name": "classic-games-bot",
  "version": "1.0.0",
  "private": true,
  "description": "Optional companion worker for the Classic Games Hub Discord bot - chat XP, live feed, stat counters and join-time role sync. Slash commands are served by the website's serverless interactions endpoint.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.7",
    "discord.js": "^14.16.3",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "@types/node": "^22.9.0"
  }
}