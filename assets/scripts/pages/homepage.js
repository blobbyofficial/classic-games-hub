window.addEventListener('DOMContentLoaded', function () {
  window.ClassicGamesHubArcade.renderLibrary({
    featuredSelector: '#featuredMount',
    mountSelector: '#catalogMount',
    filtersSelector: '#libraryFilters',
    countSelector: '#libraryCount',
    summarySelector: '#librarySummary'
  });
});
